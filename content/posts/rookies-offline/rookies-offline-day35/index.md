---
title: "[Devlog] SK 쉴더스 루키즈 5기 오프라인 세션 Day 35 — 멘토링 피드백 기반 핵심 진단 엔진(1-2, 4-5, 7-4) 고도화 및 1-6 도커 크래시 완벽 교정"
date: 2026-07-07
tags:
  - kdt
  - sk-rookies
  - sk-shieldus
  - rookies-5기
  - devlog
  - argus
  - security
  - docker
thumbnail: thumbnail.png
---

---

# 서론

> **"내일 진행될 채용 특강 세션을 앞두고, 지난 멘토링 세션에서 수령한 'Broad-to-Narrow(넓게 탐색하고 정교하게 좁히기)' 지침과 다중 권한 세션 교차 검증 피드백을 실서버 진단 엔진 코어에 전격 반영했습니다. 이번 7월 7일 스프린트에서는 4-5 일반계정 권한상승(IDOR) 세션 검증 엔진 구축, 7-4 취약한 보안설정(OSV 및 TLS 스캔) 통합, 그리고 슬림 도커(Slim Docker) 환경에서 발생하던 1-6 모듈의 크리티컬 컴파일 크래시를 완벽히 교정하여 파이프라인의 완성도를 극한으로 끌어올렸습니다."**
>
> 2026-07-07 작업 로그 기준으로 4-5 IDOR 다중 세션 검증, 7-4 OSV/TLS 통합, 1-6 슬림 Docker 컴파일 크래시 및 2-2 fan-out 최적화를 반영했습니다.

# 1. [4-5] 일반계정 권한상승 (IDOR) 다중 세션 진단 고도화 및 UI 수립

피드백에서 강조된 다중 권한 교차 주입 및 접근제어 우회 실증 원칙을 만족하기 위해, 기존의 단선적인 탐지 방식을 완전히 리팩터링했습니다.

## ── IDOR 다중 세션 시나리오 로직 이식
* **적용 소스:** `scanner.py`, `g45_discovery.py`, `g45_auth.py`
* **동작 매커니즘:** 세션 풀(Session Pool) 인프라를 기반으로 **[일반 계정 A vs 일반 계정 B vs 권한이 없는 비인증 계정(Anonymous)]**의 3대 다중 계정 시나리오 패킷을 유기적으로 교차 투사하는 아키텍처를 수립했습니다. 타인의 게시글, 주문 내역, 마스터 자산 식별자(ID) 인자를 변조 인젝션했을 때 백엔드 필터가 이를 정상 거부하는지 실시간 역추적합니다.
* **도커 네트워크 최적화:** Docker 구동 환경에 맞추어 네트워크 인터페이스 및 API Base URL 라우팅 매핑 주소를 보완하고, `/diagnosis/modules/4-5/` 파이프라인의 스캔 주기를 최적화했습니다.

## ── 프론트엔드 전용 Findings 패널 연동
* **적용 파일:** `G45FindingsPanel.tsx`, `G45SectionInfoPopover.tsx`
* **가시성 확보:** 타인 권한 탈취 결과 및 우회 성공 증적 패킷을 직관적으로 대조할 수 있도록 전용 Findings 패널과 인포 팝오버 UI 컴포넌트를 새롭게 개발하여 대시보드 단에 밀착 배치했습니다.

# 2. [1-2] 삽입 공격 엔진 고도화 및 [7-4] 취약한 보안설정 모듈 통합 완료

단계별로 쿼리를 쌓아가며 검증하는 1-2 인젝션 엔진의 하드닝과 더불어, 서버 인프라 레이어의 취약점을 전수 진단하는 7-4 모듈을 전격 결합하여 풀스택 연동을 마쳤습니다.

## ── 백엔드 코어 인프라 진단 엔진 구현 명세
* **7-4 서버 보안 설정 검사 엔진 레이어 수립:**
  * `tls_check.py` / `tlsCheck.py`: 타깃 도메인의 SSL/TLS 핸드셰이크 프로콜을 역추적하여, SK쉴더스 가이드라인 상 명백한 취약점으로 분류되는 레거시 프로토콜(SSLv3, TLS 1.0, TLS 1.1 등) 활성화 유무 및 암호화 스위트(Cipher Suite) 안전성을 판정합니다.
  * `port_scan.py`: 대상 호스트에 대해 불필요하게 대외 개방되어 공격 표면을 노출하고 있는 미사용 포트 현황을 고속 점검합니다.
  * `version_check.py`: HTTP 응답 헤더(Server, X-Powered-By 등)에서 배출되는 배너 정보를 파싱하여 소프트웨어 버전을 확인하고, 이름 및 버전 노출 시 즉각 미흡 취약점으로 확정 판정합니다.
  * `dependency_check.py` (OSV 실시간 연동): 프로젝트 빌드 자산인 `gradle_dep_files.json` 등을 파싱해 외부 오픈소스 라이브러리 의존성을 자동 수집한 뒤, **구글 OSV(Open Source Vulnerability) 데이터베이스 API를 실시간 상호 참조**하여 취약점이 실존하는 위험 버전인지 자동으로 판별해 냅니다.
* **1-2 삽입 공격 엔진 주입 속도 튜닝:**
  * `payload_injector.py`: SQL/OS 커맨드 인젝션을 유발하는 모의 페이로드 적합성을 다듬고, API 파라미터별 주입 스피드 및 예외 처리 가드를 대폭 리팩터링했습니다.
  * `zap_engine.py` / `zap_scan.py`: OWASP ZAP 액티브 스캔 정책 세션과의 연동 옵션을 튜닝하여 탐지 정확도를 상향 제고했습니다.

## ── 프론트엔드 리포트 대시보드 표출 최적화
* **적용 파일:** `DiagnosisPage.tsx`, `DiagnosisReportPanel.tsx`, `BuildSourcePanel.tsx`
* **구조화 뷰 적용:** `DiagnosisReportPanel.tsx` 구성을 개편하여 7-4 진단 엔진이 발출한 세부 지표(TLS 버전 현황, 포트 스캔 맵, 취약 라이브러리 CVE 연동 정보)를 직관적인 요약 테이블 및 차트 형태로 표시하도록 변경했습니다. 빌드 소스 패널(`BuildSourcePanel.tsx`)에는 1-2 및 7-4를 기동 제어할 수 있는 옵션 폼 설정을 최적화했습니다.

# 3. [1-6] 입력 값 크기 및 무결성 검증 오류 모듈 도커 크래시 버그 완벽 교정

도커(Docker) 컨테이너망 환경에서 1-6 진단 모듈 가동 시, 백엔드 컨테이너가 예기치 않게 다운(다운타임 발생)되거나 `ImportError`를 배출하며 파이프라인이 중단되던 크리티컬 결함을 완전히 패치했습니다.

## ── Selenium 의존성 지연 로딩 (Lazy Import) 처리
* **결함 원인:** fuzzer 엔진 내부의 `session_manager.py` 파일 최상단에서 `selenium` 패키지가 무조건적으로 즉시 import되도록 선언되어 있었습니다. 이로 인해 셀레니움 드라이버가 빌드되어 있지 않은 가벼운 **슬림 도커 백엔드 이미지 환경(Slim Docker Image)**에서 구동될 시 파일 전체가 컴파일되지 못하고 컴파일 크래시를 유발했던 것으로 규명되었습니다.
* **해결 조치:** 최상단의 무조건적 import 구문을 제거하고, 실제 브라우저 드라이버를 호출하는 기동 시점에만 패키지를 불러오도록 **`_load_selenium` 메서드를 신설하여 지연 로딩(Lazy Import) 패턴으로 코드를 전면 수정**했습니다. 이로써 해당 패키지가 없는 환경에서도 정상적인 fallback 예외 처리가 가동되어 시스템이 다운되지 않습니다.

## ── 도커 전용 설정 파일 동기화 및 라우팅 이식
* **적용 파일:** `config.docker.yaml`
* **설정 동기화:** 로컬 설정에만 존재하고 도커 구성에 누락되어 있던 `diagnosis_1_6` 블록 옵션(`skip_zap`, `skip_spider`, `skip_selenium` 등 변수 팩터)을 동기화 지정하여 컨테이너망에서도 올바른 스킵 제어가 수행되도록 보정했습니다. 컨테이너 외부 호스트 서버로의 트래픽 라우팅을 보장하는 `ARGUS_PROBE_HOST` 주소 재작성(Rewrite) 규칙을 1-6 스캐너 프로브 단에도 전향 적용했습니다.

# 4. [2-2] 중요 정보 파일 다운로드 엔드포인트 전파 (Fan-out) 최적화

* **다중 Base URL 대응 검사 다변화 (`transfer_endpoints_service.py`):** 2-2 파일 다운로드 및 상위 경로 변조 진단 시, 특정 특정 벤더 경로(`/user-api`)에만 의존하여 쿼리를 송신하던 레거시 정적 재작성(Rewriting) 방식을 완전히 전면 폐기했습니다.
* **검사 커버리지 확장:** 대시보드 시스템에 등록된 모든 `base-urls` Origin 대역으로 탐지된 다운로드 URL 엔드포인트를 **동시 전파(Fan out) 투사하는 분기 알고리즘을 이식**하여 스캔 타깃 커버리지를 대폭 확장했습니다. 도커 내부 프로브 오라우팅을 방지하기 위해 `probe_base_url` 헬퍼를 결합하여 다운로드 결함 증적의 URL 주소 체계를 표준 정규화했습니다.

# 5. [1-1] XSS / CSRF 도커 환경 경로 매핑 수정 및 UI 개선

* **도커 전용 경로 빌드 로직 수립:** 도커 컨테이너 내부망 환경 하에서 ZAP 엔진 실행 시 생성되는 파일 정보 및 리포트 저장 경로가 호스트 OS 로컬 환경의 경로 정보와 충돌을 일으키며 불일치하던 싱크 현상을 교정하기 위해, `backend/app/config.py` 소스코드 단에 **도커 컨테이너 전용 경로 동적 빌드 아키텍처**를 추가 삽입했습니다.
* **텍스트 오버플로우 패치:** 1-1 진단 완료 후 대시보드 리포트 상세 장표 페이지에서 긴 페이로드 설명문 인입 시 줄바꿈 가드가 무력화되어 텍스트 렌더링이 잘리던 프론트엔드 스타일 유실 오류를 정밀 수정했습니다.

# 6. 공통 헬퍼 개선 및 시스템 진득도 모니터링 안정화

* **범용 키워드 아규먼트 (`**kwargs`) 도입 수립:** 중앙 코어 서비스 스케줄러인 `run_section` 호출부 구문에서 특정 모듈 변수의 부가 인자 전달이 누락될 시 발생하던 치명적인 `TypeError` 예외 마비 현상을 차단하기 위해, 백엔드 핵심 구동 파이프라인 흐름 함수 시그니처 래퍼 전반에 `**kwargs` 범용 인자 처리 가드를 주입했습니다.
* **실시간 폴링 레이어 하드닝 (`progress_reporter.py`):** 1-2 인젝션 및 4-5 IDOR 스캔 등 대량의 패킷 전송으로 인해 장시간 수행되는 백엔드 진단 모듈의 실시간 진척도(Progress) 지표가 대시보드 단으로 끊김 없이 웹소켓 및 폴링 응답을 통해 전달되도록 수신 커넥션 로직의 전송 안정성을 다듬었습니다.

# 7. Next Milestone: 채용 특강 세션 수렴 및 스크린샷 증적 캡처 엔진 빌드 진입

오늘 자 작업을 통해 멘토링 피드백에 입각한 코어 탐지 로직 수정과 결함 전파, 그리고 도커 환경 하에서의 컴파일 크래시 버그까지 완전히 하드닝 정비 완료했습니다. 이로써 플랫폼 검색 엔진 및 통제 인프라 결합을 수립하는 스프린트 1 단계의 기술적 요구사항이 성공적으로 마감되었습니다.

* **Next Milestone 세부 계획:**
  1. **현업 채용 특강 세션 수렴 (7/8):** 내일은 플랫폼 개발 공정을 잠시 세션 멈춤 처리하고, 인사 거버넌스 및 취업 역량 강화를 위한 전사 채용 특강 일정을 충실히 수렴할 예정입니다. 대기업 보안 생태계 동향과 HR 관점의 소프트 스킬 평정 요소를 파악하여 향후 플랫폼 런칭 방향성에 조율합니다.
  2. **Playwright 기반 실시간 증적 스크린샷 캡처 선택형 모듈 구현 (7/9 ~ 7/11):** 채용 특강 마감 직후, 아르고스 플랫폼의 가장 핵심적인 비주얼 하이라이트인 `backend/screenshot/modules/{id}` 동적 증적 채취 엔진 구축 스프린트에 전격 돌입합니다. 스캐너 타격 시의 트래픽 추이를 포착하는 과정(Process) 캡처 모드와 최종 익스플로잇 성공 시점의 Before-After 대조 장표를 찍어내는 결과(Result) 캡처 모드의 선택형 셀레니움/Playwright 헤드리스 드라이버 통제 로직 코딩을 집행할 계획입니다. 차기 스프린트에서 이어서 계속 진행합니다.
