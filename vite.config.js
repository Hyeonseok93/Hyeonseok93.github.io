import { defineConfig } from 'vite';
import { resolve } from 'path';

/** Dev server: stale gh-pages HTML may still point at /assets/main.js and /style.css */
function devGhPagesAssetFallback() {
  return {
    name: 'dev-gh-pages-asset-fallback',
    configureServer(server) {
      server.middlewares.use((req, _res, next) => {
        const url = req.url?.split('?')[0];
        if (url === '/assets/main.js') req.url = '/src/main.js';
        else if (url === '/style.css') req.url = '/src/style.css';
        next();
      });
    },
  };
}

// index.html is gitignored and generated by `npm run build:html:vite-index`.
export default defineConfig({
  plugins: [devGhPagesAssetFallback()],
  build: {
    outDir: 'dist',
    assetsDir: 'images',
    rollupOptions: {
      input: {
        main: resolve(__dirname, 'index.html'),
      },
      output: {
        assetFileNames: (assetInfo) => {
          if (assetInfo.name === 'main.css' || assetInfo.name === 'style.css') {
            return 'style.css';
          }
          return 'images/[name].[ext]';
        },
        entryFileNames: 'assets/[name].js',
        chunkFileNames: 'assets/[name].js',
      },
    },
  },
});
